import app from '../App.js';

// Vercel's Node.js runtime accepts any (req, res) handler as the default
// export — an Express app satisfies that signature directly, so no
// adapter is needed here.
export default app;